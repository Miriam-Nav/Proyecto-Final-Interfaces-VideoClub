<Window x:Class="View.ConfiguracionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Configuración de Géneros y Tarifas - VideoClub" 
        Height="680" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
            <GradientStop Color="#E3FFCC" Offset="0"/>
            <GradientStop Color="#FFFFFF" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Estilos.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource BarraSup}" Grid.Row="0">
            <DockPanel>
                <TextBlock Text="Configuración de Géneros y Tarifas" Style="{StaticResource TituloBarraStyle}" DockPanel.Dock="Left"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource TarjetaStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Gestión de Géneros" Style="{StaticResource TituloTarjetaStyle}" Grid.Row="0"/>

                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="Nombre del Género" Style="{StaticResource LabelStyle}"/>
                        <DockPanel>
                            <TextBox Text="{Binding InputNombreGenero, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}"/>
                        </DockPanel>
                        <TextBlock Text="{Binding ErrorGenero}" Style="{StaticResource ErrorMensajeStyle}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Generos}" SelectedItem="{Binding GeneroSeleccionado}" Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                            <DataGridTextColumn Header="Género" Binding="{Binding Nombre}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Eliminar Género" Command="{Binding EliminarGeneroCommand}" Style="{StaticResource ErrorButtonStyle}" Width="150" Margin="10,0,0,0"/>
                        <Button Content="Guardar Género" Command="{Binding GuardarGeneroCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="150" Margin="10,0,0,0"/>
                        
                    </StackPanel>
                    
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource TarjetaStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Gestión de Tarifas" Style="{StaticResource TituloTarjetaStyle}" Grid.Row="0"/>

                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,5,0">
                                <TextBlock Text="Descripción" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding InputDescripcionTarifa, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Margin="5,0,0,0">
                                <TextBlock Text="Precio (€/Día)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding InputPrecioDia, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}"/>
                            </StackPanel>
                        </UniformGrid>

                        <TextBlock Text="{Binding ErrorTarifa}" Style="{StaticResource ErrorMensajeStyle}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Tarifas}" SelectedItem="{Binding TarifaSeleccionada}" Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding Descripcion}" Width="*"/>
                            <DataGridTextColumn Header="Precio" Binding="{Binding PrecioDia, StringFormat={}{0:N2} €}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Eliminar Tarifa" Command="{Binding EliminarTarifaCommand}" Style="{StaticResource ErrorButtonStyle}" Width="150" Margin="10,0,0,0"/>
                        <Button Content="Guardar Tarifa" Command="{Binding GuardarTarifaCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="150" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>