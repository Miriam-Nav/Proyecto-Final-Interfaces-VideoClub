<Window x:Class="View.PrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gestión VideoClub" 
        Height="680" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
            <GradientStop Color="#E3FFCC" Offset="0"/>
            <GradientStop Color="#FFFFFF" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <ResourceDictionary Source="Estilos.xaml"/>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="#1A1A1A" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="10,5">
            <MenuItem Header="Clientes" Command="{Binding AbrirClientesCommand}" Style="{StaticResource MenuPrincipalStyle}"/>
            <MenuItem Header="Catálogo" Command="{Binding AbrirCatalogoCommand}" Style="{StaticResource MenuPrincipalStyle}"/>
            <MenuItem Header="Alquileres" Command="{Binding AbrirAlquileresCommand}" Style="{StaticResource MenuPrincipalStyle}"/>
            <MenuItem Header="Configuración" Command="{Binding AbrirConfigCommand}" Style="{StaticResource MenuPrincipalStyle}"/>
        </Menu>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Resumen General" Style="{StaticResource TituloTarjetaStyle}" FontSize="24"/>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,20">
                <Border Style="{StaticResource TarjetaStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Total Clientes" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding TotalClientes}" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource TarjetaStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Productos" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding TotalProductos}" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource TarjetaStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Alquileres Activos" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding AlquileresActivos}" FontSize="28" FontWeight="Bold" Foreground="#27AE60"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource TarjetaStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Stock Bajo" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding StockBajo}" FontSize="28" FontWeight="Bold" Foreground="#E74C3C"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="2" Style="{StaticResource ListaStyle}">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Últimos Alquileres Registrados" Style="{StaticResource TituloTarjetaStyle}" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding ActividadReciente}" Style="{StaticResource DataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding Clientes.Nombre}" Width="200"/>
                            <DataGridTextColumn Header="Producto" Binding="{Binding Productos.Titulo}" Width="150"/>
                            <DataGridTextColumn Header="F. Salida" Binding="{Binding FechaSalida, StringFormat=\{0:dd/MM/yyyy\}}" Width="*"/>
                            <DataGridTextColumn Header="F. Prevista" Binding="{Binding FechaPrevistaDevolucion, StringFormat=\{0:dd/MM/yyyy\}}" Width="*"/>
                            <DataGridTextColumn Header="F. Devolución Real" Binding="{Binding FechaDevolucionReal, StringFormat=\{0:dd/MM/yyyy\}}" Width="*"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding CosteTotal, StringFormat={}{0:N2} €}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>